#ifndef GLOBAL
#define GLOBAL
#define MAX_ITER_BASE 100

extern int MAX_ITER = MAX_ITER_BASE;
extern const int ref_zoom = 2; // distance on the complex plane at zoom x1
extern int dim_x = 1100; // x and y dims of screen
extern int dim_y = 1100;
extern const int NUM_THREADS = 128; // num threads

# endif